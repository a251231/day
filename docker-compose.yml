version: "3.9"
services:
  wecom-report-ui:
    image: python:3.10-slim
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "0.0.0.0:8501:8501"
    environment:
      WE_COM_REPORT_UI_LAUNCHER: "1"
    command: >
      bash -lc "pip install -r requirements.txt
      && streamlit run wecom_report_ui.py
      --server.address=0.0.0.0
      --server.port=8501
      --server.headless=true
      --browser.gatherUsageStats=false"
